# zenbu-labs.terminal-browser

Plugin config directory (managed via Stow).

Upstream plugin currently has no user config file; keybinding lives in
`~/.config/herdr/config.toml` (`prefix+shift+b` → `open-split`).

Docs: https://github.com/zenbu-labs/terminal-browser/tree/main/herdr-plugin
